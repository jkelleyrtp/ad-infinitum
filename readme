fn main() {
    let mut count: [u8; 4] = [0, 0, 0, 0];
    let target: [u8; 4] = [u8::MAX, u8::MAX, u8::MAX, u8::MAX];

    while count != target {
        for i in &mut count {
            *i = i.wrapping_add(1);
            if *i != 0 {
                break;
            }
        }
    }
}
